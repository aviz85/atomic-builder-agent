---
description: Score the project across all 7 Atomic Business layers
argument-hint: '[optional focus]'
allowed-tools: Read, Write, Edit
---

# /score

Use `layer-status-review` to score the current state from 0 to 5 for every layer.

Raw focus:
`$ARGUMENTS`

Update `data/layer-status.json`.
